using UnityEngine;
using TMPro;

public class UIPlayerStats : MonoBehaviour
{
   /* [SerializeField] private TextMeshProUGUI targetsText;
    private int targets = 0;

    private void OnEnable()
    {
        TargetHitMulti.OnTargetHit += ChangeTargetsText;
    }

    private void OnDisable()
    {
        TargetHitMulti.OnTargetHit -= ChangeTargetsText;
    }

    private void ChangeTargetsText(int targetIncrement)
    {
        // Increment the targets count and update the UI
        targets += targetIncrement;
        targetsText.text = "Targets: " + targets;
    }*/
}
